# --- Frameworks & Server ---
fastapi
uvicorn
python-multipart
gunicorn==23.0.0
eventlet

# --- Socket & Communication ---
python-socketio
flask-socketio

# --- Database & Config ---
sqlalchemy
python-dotenv
psycopg2-binary  # 🛡️ ท่านจอมพลต้องเพิ่มตัวนี้สำหรับเชื่อมต่อ PostgreSQL ครับ!

# --- AI & Processing ---
# torch        <-- ❌ ลบทิ้ง (หนักเกินไปสำหรับ Render Free)
# transformers <-- ❌ ลบทิ้ง (หนักเกินไปสำหรับ Render Free)
numpy==1.26.4
google-generativeai

# --- Utilities ---
pydantic
pydantic-settings
requests
aiohttp
# pymysql      <-- ❌ ถ้าใช้ PostgreSQL ไม่ต้องใช้ตัวนี้ครับ
