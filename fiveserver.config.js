// fiveserver.config.js
module.exports = {
    // 1. จุดที่สำคัญที่สุด: สั่งให้หยุดรีเฟรชเมื่อไฟล์เหล่านี้เปลี่ยน
    ignore: [
        /\.json$/,           // ปิดการตรวจจับไฟล์ .json ทั้งหมด
        '**/data/**',        // ปิดการตรวจจับในโฟลเดอร์ data
        'latest_strike.json',// ระบุเจาะจงไฟล์เจ้าปัญหา
        '**/*.log'           // ป้องกันไฟล์ log ทำให้รีเฟรชด้วยครับ
    ],

    // 2. ตั้งค่าเสริมเพื่อความสะดวก (ใส่หรือไม่ใส่ก็ได้)
    port: 5500,            // ล็อกพอร์ตไว้ที่ 5500 จะได้ไม่ต้องเปลี่ยนลิงก์บ่อย
    highlight: true,       // ไฮไลท์จุดที่แก้ไขในหน้าเว็บ (ถ้ามีการแก้ HTML/CSS)
    injectBody: true       // ช่วยให้การเชื่อมต่อระหว่าง Browser กับ Server เสถียรขึ้น
};